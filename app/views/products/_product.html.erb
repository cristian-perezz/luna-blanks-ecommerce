<div class="bg-white p-4 rounded-xl shadow">
  <h2 class="text-lg font-bold"><%= product.name %></h2>
  <p class="text-gray-600">$<%= product.price %></p>

  <%= link_to "View", product_path(product), class: "text-blue-500" %>
</div>